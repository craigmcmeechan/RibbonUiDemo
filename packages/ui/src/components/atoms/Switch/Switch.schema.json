{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ribbon-ui:schema:component:switch:1.0.0",
  "title": "SwitchConfig",
  "description": "Serializable configuration for the RibbonUI switch atom.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "label", "checked"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "Stable switch instance identifier."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "Visible text that supplies the switch accessible name."
    },
    "checked": {
      "type": "boolean",
      "description": "Controlled on/off state rendered as aria-checked; the host owns the value."
    },
    "disabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether native activation and focus participation are disabled."
    },
    "size": {
      "type": "string",
      "enum": ["small", "medium", "large"],
      "default": "medium",
      "description": "Explicit control density and target size."
    }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ribbon-ui:schema:component:button:1.0.0",
  "title": "ButtonConfig",
  "description": "Serializable configuration for the accessible RibbonUI button atom.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "label"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "Stable button instance identifier."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "Visible text that also supplies the button's accessible name."
    },
    "variant": {
      "type": "string",
      "enum": ["neutral", "primary"],
      "default": "neutral",
      "description": "Semantic visual emphasis of the button."
    },
    "size": {
      "type": "string",
      "enum": ["small", "medium", "large"],
      "default": "medium",
      "description": "Explicit button density and target size."
    },
    "disabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether native activation and focus participation are disabled."
    }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ribbon-ui:schema:component:dropdown:1.0.0",
  "title": "DropdownConfig",
  "description": "Serializable configuration for the RibbonUI dropdown primitive.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "label"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "Stable dropdown instance identifier."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "Visible text of the trigger button and its accessible name."
    },
    "placement": {
      "type": "string",
      "enum": ["bottom", "top"],
      "default": "bottom",
      "description": "Vertical placement of the menu popover relative to the trigger."
    },
    "disabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether the trigger is disabled and cannot open the menu."
    }
  }
}

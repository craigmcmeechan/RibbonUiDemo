{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ribbon-ui:schema:component:ribbon-tab:1.0.0",
  "title": "RibbonTabConfig",
  "description": "Serializable configuration for the RibbonUI ribbon tab primitive.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "label"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "Stable ribbon tab instance identifier."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "Visible text of the tab button and its accessible name."
    }
  }
}

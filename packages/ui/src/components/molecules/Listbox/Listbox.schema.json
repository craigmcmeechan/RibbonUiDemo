{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ribbon-ui:schema:component:listbox:1.0.0",
  "title": "ListboxConfig",
  "description": "Serializable configuration for the RibbonUI listbox primitive.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "Stable listbox instance identifier."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "Optional accessible name for the listbox."
    }
  }
}
